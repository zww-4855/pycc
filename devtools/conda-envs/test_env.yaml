name: test
channels:

  - conda-forge

  - defaults
dependencies:
    # Base depends
  - python
  - pip
  - scipy
  - numpy 

    # Testing
  - pytest
  - pytest-cov
  - codecov
    #  - pyscf

    # Pip-only installs
  - pip:
     - pyscf
  #  - codecov

